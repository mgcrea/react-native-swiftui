///
/// HybridPickerViewComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "HybridPickerViewComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <NitroModules/PropNameIDCache.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::rnswiftui::views {

  extern const char HybridPickerViewComponentName[] = "PickerView";

  HybridPickerViewProps::HybridPickerViewProps(const react::PropsParserContext& context,
                                               const HybridPickerViewProps& sourceProps,
                                               const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    value([&]() -> CachedProp<std::optional<std::string>> {
      try {
        const react::RawValue* rawValue = rawProps.at("value", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.value;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::string>>::fromRawValue(*runtime, value, sourceProps.value);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PickerView.value: ") + exc.what());
      }
    }()),
    selection([&]() -> CachedProp<std::optional<std::string>> {
      try {
        const react::RawValue* rawValue = rawProps.at("selection", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.selection;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::string>>::fromRawValue(*runtime, value, sourceProps.selection);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PickerView.selection: ") + exc.what());
      }
    }()),
    label([&]() -> CachedProp<std::optional<std::string>> {
      try {
        const react::RawValue* rawValue = rawProps.at("label", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.label;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::string>>::fromRawValue(*runtime, value, sourceProps.label);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PickerView.label: ") + exc.what());
      }
    }()),
    labelColor([&]() -> CachedProp<std::optional<std::string>> {
      try {
        const react::RawValue* rawValue = rawProps.at("labelColor", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.labelColor;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::string>>::fromRawValue(*runtime, value, sourceProps.labelColor);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PickerView.labelColor: ") + exc.what());
      }
    }()),
    options([&]() -> CachedProp<std::optional<std::vector<PickerViewOption>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("options", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.options;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::vector<PickerViewOption>>>::fromRawValue(*runtime, value, sourceProps.options);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PickerView.options: ") + exc.what());
      }
    }()),
    pickerStyle([&]() -> CachedProp<std::optional<std::string>> {
      try {
        const react::RawValue* rawValue = rawProps.at("pickerStyle", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.pickerStyle;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::string>>::fromRawValue(*runtime, value, sourceProps.pickerStyle);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PickerView.pickerStyle: ") + exc.what());
      }
    }()),
    controlSize([&]() -> CachedProp<std::optional<std::string>> {
      try {
        const react::RawValue* rawValue = rawProps.at("controlSize", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.controlSize;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::string>>::fromRawValue(*runtime, value, sourceProps.controlSize);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PickerView.controlSize: ") + exc.what());
      }
    }()),
    disabled([&]() -> CachedProp<std::optional<bool>> {
      try {
        const react::RawValue* rawValue = rawProps.at("disabled", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.disabled;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<bool>>::fromRawValue(*runtime, value, sourceProps.disabled);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PickerView.disabled: ") + exc.what());
      }
    }()),
    onNativeChange([&]() -> CachedProp<std::optional<std::function<void(const std::string& /* value */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onNativeChange", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onNativeChange;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::string& /* value */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onNativeChange);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PickerView.onNativeChange: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridPickerViewSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridPickerViewSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PickerView.hybridRef: ") + exc.what());
      }
    }()) { }

  bool HybridPickerViewProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("value"): return true;
      case hashString("selection"): return true;
      case hashString("label"): return true;
      case hashString("labelColor"): return true;
      case hashString("options"): return true;
      case hashString("pickerStyle"): return true;
      case hashString("controlSize"): return true;
      case hashString("disabled"): return true;
      case hashString("onNativeChange"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridPickerViewComponentDescriptor::HybridPickerViewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  std::shared_ptr<const react::Props> HybridPickerViewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                      const std::shared_ptr<const react::Props>& props,
                                                                                      react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridPickerViewShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridPickerViewComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = static_cast<HybridPickerViewShadowNode&>(shadowNode);
    const std::shared_ptr<const HybridPickerViewProps>& constProps = concreteShadowNode.getConcreteSharedProps();
    const std::shared_ptr<HybridPickerViewProps>& props = std::const_pointer_cast<HybridPickerViewProps>(constProps);
    HybridPickerViewState state{props};
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::rnswiftui::views
