///
/// HybridSheetViewComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "HybridSheetViewComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <NitroModules/PropNameIDCache.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::rnswiftui::views {

  extern const char HybridSheetViewComponentName[] = "SheetView";

  HybridSheetViewProps::HybridSheetViewProps(const react::PropsParserContext& context,
                                             const HybridSheetViewProps& sourceProps,
                                             const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    isPresented([&]() -> CachedProp<std::optional<bool>> {
      try {
        const react::RawValue* rawValue = rawProps.at("isPresented", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.isPresented;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<bool>>::fromRawValue(*runtime, value, sourceProps.isPresented);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SheetView.isPresented: ") + exc.what());
      }
    }()),
    detents([&]() -> CachedProp<std::optional<std::vector<std::string>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("detents", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.detents;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::vector<std::string>>>::fromRawValue(*runtime, value, sourceProps.detents);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SheetView.detents: ") + exc.what());
      }
    }()),
    title([&]() -> CachedProp<std::optional<std::string>> {
      try {
        const react::RawValue* rawValue = rawProps.at("title", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.title;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::string>>::fromRawValue(*runtime, value, sourceProps.title);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SheetView.title: ") + exc.what());
      }
    }()),
    message([&]() -> CachedProp<std::optional<std::string>> {
      try {
        const react::RawValue* rawValue = rawProps.at("message", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.message;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::string>>::fromRawValue(*runtime, value, sourceProps.message);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SheetView.message: ") + exc.what());
      }
    }()),
    primaryButtonTitle([&]() -> CachedProp<std::optional<std::string>> {
      try {
        const react::RawValue* rawValue = rawProps.at("primaryButtonTitle", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.primaryButtonTitle;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::string>>::fromRawValue(*runtime, value, sourceProps.primaryButtonTitle);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SheetView.primaryButtonTitle: ") + exc.what());
      }
    }()),
    secondaryButtonTitle([&]() -> CachedProp<std::optional<std::string>> {
      try {
        const react::RawValue* rawValue = rawProps.at("secondaryButtonTitle", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.secondaryButtonTitle;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::string>>::fromRawValue(*runtime, value, sourceProps.secondaryButtonTitle);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SheetView.secondaryButtonTitle: ") + exc.what());
      }
    }()),
    onNativeDismiss([&]() -> CachedProp<std::optional<std::function<void()>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onNativeDismiss", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onNativeDismiss;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void()>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onNativeDismiss);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SheetView.onNativeDismiss: ") + exc.what());
      }
    }()),
    onNativePrimaryAction([&]() -> CachedProp<std::optional<std::function<void()>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onNativePrimaryAction", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onNativePrimaryAction;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void()>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onNativePrimaryAction);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SheetView.onNativePrimaryAction: ") + exc.what());
      }
    }()),
    onNativeSecondaryAction([&]() -> CachedProp<std::optional<std::function<void()>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onNativeSecondaryAction", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onNativeSecondaryAction;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void()>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onNativeSecondaryAction);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SheetView.onNativeSecondaryAction: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridSheetViewSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridSheetViewSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SheetView.hybridRef: ") + exc.what());
      }
    }()) { }

  bool HybridSheetViewProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("isPresented"): return true;
      case hashString("detents"): return true;
      case hashString("title"): return true;
      case hashString("message"): return true;
      case hashString("primaryButtonTitle"): return true;
      case hashString("secondaryButtonTitle"): return true;
      case hashString("onNativeDismiss"): return true;
      case hashString("onNativePrimaryAction"): return true;
      case hashString("onNativeSecondaryAction"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridSheetViewComponentDescriptor::HybridSheetViewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  std::shared_ptr<const react::Props> HybridSheetViewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                     const std::shared_ptr<const react::Props>& props,
                                                                                     react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridSheetViewShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridSheetViewComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = static_cast<HybridSheetViewShadowNode&>(shadowNode);
    const std::shared_ptr<const HybridSheetViewProps>& constProps = concreteShadowNode.getConcreteSharedProps();
    const std::shared_ptr<HybridSheetViewProps>& props = std::const_pointer_cast<HybridSheetViewProps>(constProps);
    HybridSheetViewState state{props};
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::rnswiftui::views
