///
/// HybridSheetPickerViewComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "HybridSheetPickerViewComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <NitroModules/PropNameIDCache.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::rnswiftui::views {

  extern const char HybridSheetPickerViewComponentName[] = "SheetPickerView";

  HybridSheetPickerViewProps::HybridSheetPickerViewProps(const react::PropsParserContext& context,
                                                         const HybridSheetPickerViewProps& sourceProps,
                                                         const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    isPresented([&]() -> CachedProp<std::optional<bool>> {
      try {
        const react::RawValue* rawValue = rawProps.at("isPresented", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.isPresented;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<bool>>::fromRawValue(*runtime, value, sourceProps.isPresented);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SheetPickerView.isPresented: ") + exc.what());
      }
    }()),
    title([&]() -> CachedProp<std::optional<std::string>> {
      try {
        const react::RawValue* rawValue = rawProps.at("title", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.title;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::string>>::fromRawValue(*runtime, value, sourceProps.title);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SheetPickerView.title: ") + exc.what());
      }
    }()),
    searchPlaceholder([&]() -> CachedProp<std::optional<std::string>> {
      try {
        const react::RawValue* rawValue = rawProps.at("searchPlaceholder", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.searchPlaceholder;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::string>>::fromRawValue(*runtime, value, sourceProps.searchPlaceholder);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SheetPickerView.searchPlaceholder: ") + exc.what());
      }
    }()),
    selectedValue([&]() -> CachedProp<std::optional<std::string>> {
      try {
        const react::RawValue* rawValue = rawProps.at("selectedValue", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.selectedValue;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::string>>::fromRawValue(*runtime, value, sourceProps.selectedValue);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SheetPickerView.selectedValue: ") + exc.what());
      }
    }()),
    options([&]() -> CachedProp<std::optional<std::vector<SheetPickerViewOption>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("options", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.options;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::vector<SheetPickerViewOption>>>::fromRawValue(*runtime, value, sourceProps.options);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SheetPickerView.options: ") + exc.what());
      }
    }()),
    autoDismiss([&]() -> CachedProp<std::optional<bool>> {
      try {
        const react::RawValue* rawValue = rawProps.at("autoDismiss", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.autoDismiss;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<bool>>::fromRawValue(*runtime, value, sourceProps.autoDismiss);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SheetPickerView.autoDismiss: ") + exc.what());
      }
    }()),
    onNativeSelect([&]() -> CachedProp<std::optional<std::function<void(const std::string& /* value */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onNativeSelect", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onNativeSelect;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::string& /* value */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onNativeSelect);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SheetPickerView.onNativeSelect: ") + exc.what());
      }
    }()),
    onNativeDismiss([&]() -> CachedProp<std::optional<std::function<void()>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onNativeDismiss", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onNativeDismiss;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void()>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onNativeDismiss);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SheetPickerView.onNativeDismiss: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridSheetPickerViewSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridSheetPickerViewSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SheetPickerView.hybridRef: ") + exc.what());
      }
    }()) { }

  bool HybridSheetPickerViewProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("isPresented"): return true;
      case hashString("title"): return true;
      case hashString("searchPlaceholder"): return true;
      case hashString("selectedValue"): return true;
      case hashString("options"): return true;
      case hashString("autoDismiss"): return true;
      case hashString("onNativeSelect"): return true;
      case hashString("onNativeDismiss"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridSheetPickerViewComponentDescriptor::HybridSheetPickerViewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  std::shared_ptr<const react::Props> HybridSheetPickerViewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                           const std::shared_ptr<const react::Props>& props,
                                                                                           react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridSheetPickerViewShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridSheetPickerViewComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = static_cast<HybridSheetPickerViewShadowNode&>(shadowNode);
    const std::shared_ptr<const HybridSheetPickerViewProps>& constProps = concreteShadowNode.getConcreteSharedProps();
    const std::shared_ptr<HybridSheetPickerViewProps>& props = std::const_pointer_cast<HybridSheetPickerViewProps>(constProps);
    HybridSheetPickerViewState state{props};
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::rnswiftui::views
