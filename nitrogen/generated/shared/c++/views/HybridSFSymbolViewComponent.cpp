///
/// HybridSFSymbolViewComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "HybridSFSymbolViewComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <NitroModules/PropNameIDCache.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::rnswiftui::views {

  extern const char HybridSFSymbolViewComponentName[] = "SFSymbolView";

  HybridSFSymbolViewProps::HybridSFSymbolViewProps(const react::PropsParserContext& context,
                                                   const HybridSFSymbolViewProps& sourceProps,
                                                   const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    symbolName([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("symbolName", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.symbolName;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.symbolName);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SFSymbolView.symbolName: ") + exc.what());
      }
    }()),
    size([&]() -> CachedProp<std::optional<double>> {
      try {
        const react::RawValue* rawValue = rawProps.at("size", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.size;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<double>>::fromRawValue(*runtime, value, sourceProps.size);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SFSymbolView.size: ") + exc.what());
      }
    }()),
    textStyle([&]() -> CachedProp<std::optional<std::string>> {
      try {
        const react::RawValue* rawValue = rawProps.at("textStyle", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.textStyle;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::string>>::fromRawValue(*runtime, value, sourceProps.textStyle);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SFSymbolView.textStyle: ") + exc.what());
      }
    }()),
    weight([&]() -> CachedProp<std::optional<std::string>> {
      try {
        const react::RawValue* rawValue = rawProps.at("weight", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.weight;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::string>>::fromRawValue(*runtime, value, sourceProps.weight);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SFSymbolView.weight: ") + exc.what());
      }
    }()),
    scale([&]() -> CachedProp<std::optional<std::string>> {
      try {
        const react::RawValue* rawValue = rawProps.at("scale", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.scale;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::string>>::fromRawValue(*runtime, value, sourceProps.scale);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SFSymbolView.scale: ") + exc.what());
      }
    }()),
    renderingMode([&]() -> CachedProp<std::optional<std::string>> {
      try {
        const react::RawValue* rawValue = rawProps.at("renderingMode", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.renderingMode;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::string>>::fromRawValue(*runtime, value, sourceProps.renderingMode);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SFSymbolView.renderingMode: ") + exc.what());
      }
    }()),
    variableValue([&]() -> CachedProp<std::optional<double>> {
      try {
        const react::RawValue* rawValue = rawProps.at("variableValue", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.variableValue;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<double>>::fromRawValue(*runtime, value, sourceProps.variableValue);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SFSymbolView.variableValue: ") + exc.what());
      }
    }()),
    colors([&]() -> CachedProp<std::optional<std::vector<std::string>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("colors", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.colors;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::vector<std::string>>>::fromRawValue(*runtime, value, sourceProps.colors);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SFSymbolView.colors: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridSFSymbolViewSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridSFSymbolViewSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SFSymbolView.hybridRef: ") + exc.what());
      }
    }()) { }

  bool HybridSFSymbolViewProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("symbolName"): return true;
      case hashString("size"): return true;
      case hashString("textStyle"): return true;
      case hashString("weight"): return true;
      case hashString("scale"): return true;
      case hashString("renderingMode"): return true;
      case hashString("variableValue"): return true;
      case hashString("colors"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridSFSymbolViewComponentDescriptor::HybridSFSymbolViewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  std::shared_ptr<const react::Props> HybridSFSymbolViewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                        const std::shared_ptr<const react::Props>& props,
                                                                                        react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridSFSymbolViewShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridSFSymbolViewComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = static_cast<HybridSFSymbolViewShadowNode&>(shadowNode);
    const std::shared_ptr<const HybridSFSymbolViewProps>& constProps = concreteShadowNode.getConcreteSharedProps();
    const std::shared_ptr<HybridSFSymbolViewProps>& props = std::const_pointer_cast<HybridSFSymbolViewProps>(constProps);
    HybridSFSymbolViewState state{props};
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::rnswiftui::views
