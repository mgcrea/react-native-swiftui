///
/// HybridSheetPickerViewComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridSheetPickerViewComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridSheetPickerViewSpecSwift.hpp"
#import "RNSwiftUI-Swift-Cxx-Umbrella.hpp"

#if __has_include(<cxxreact/ReactNativeVersion.h>)
#include <cxxreact/ReactNativeVersion.h>
#if REACT_NATIVE_VERSION_MINOR >= 82
#define ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
#endif
#endif

using namespace facebook;
using namespace margelo::nitro::rnswiftui;
using namespace margelo::nitro::rnswiftui::views;

/**
 * Represents the React Native View holder for the Nitro "SheetPickerView" HybridView.
 */
@interface HybridSheetPickerViewComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridSheetPickerViewComponent {
  std::shared_ptr<HybridSheetPickerViewSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridSheetPickerViewComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridSheetPickerViewComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridSheetPickerViewSpec> hybridView = RNSwiftUI::RNSwiftUIAutolinking::createSheetPickerView();
    _hybridView = std::dynamic_pointer_cast<HybridSheetPickerViewSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  RNSwiftUI::HybridSheetPickerViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridSheetPickerViewProps const>(props);
  auto& newViewProps = const_cast<HybridSheetPickerViewProps&>(newViewPropsConst);
  RNSwiftUI::HybridSheetPickerViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // isPresented: optional
  if (newViewProps.isPresented.isDirty) {
    swiftPart.setIsPresented(newViewProps.isPresented.value);
    newViewProps.isPresented.isDirty = false;
  }
  // title: optional
  if (newViewProps.title.isDirty) {
    swiftPart.setTitle(newViewProps.title.value);
    newViewProps.title.isDirty = false;
  }
  // searchPlaceholder: optional
  if (newViewProps.searchPlaceholder.isDirty) {
    swiftPart.setSearchPlaceholder(newViewProps.searchPlaceholder.value);
    newViewProps.searchPlaceholder.isDirty = false;
  }
  // selectedValue: optional
  if (newViewProps.selectedValue.isDirty) {
    swiftPart.setSelectedValue(newViewProps.selectedValue.value);
    newViewProps.selectedValue.isDirty = false;
  }
  // options: optional
  if (newViewProps.options.isDirty) {
    swiftPart.setOptions(newViewProps.options.value);
    newViewProps.options.isDirty = false;
  }
  // autoDismiss: optional
  if (newViewProps.autoDismiss.isDirty) {
    swiftPart.setAutoDismiss(newViewProps.autoDismiss.value);
    newViewProps.autoDismiss.isDirty = false;
  }
  // onNativeSelect: optional
  if (newViewProps.onNativeSelect.isDirty) {
    swiftPart.setOnNativeSelect(newViewProps.onNativeSelect.value);
    newViewProps.onNativeSelect.isDirty = false;
  }
  // onNativeDismiss: optional
  if (newViewProps.onNativeDismiss.isDirty) {
    swiftPart.setOnNativeDismiss(newViewProps.onNativeDismiss.value);
    newViewProps.onNativeDismiss.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return RNSwiftUI::RNSwiftUIAutolinking::isSheetPickerViewRecyclable();
}

- (void)prepareForRecycle {
  [super prepareForRecycle];
  RNSwiftUI::HybridSheetPickerViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

#ifdef ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
- (void)invalidate {
  RNSwiftUI::HybridSheetPickerViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.onDropView();
  [super invalidate];
}
#endif

@end
