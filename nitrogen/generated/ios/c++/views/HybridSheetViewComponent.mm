///
/// HybridSheetViewComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridSheetViewComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridSheetViewSpecSwift.hpp"
#import "RNSwiftUI-Swift-Cxx-Umbrella.hpp"

using namespace facebook;
using namespace margelo::nitro::rnswiftui;
using namespace margelo::nitro::rnswiftui::views;

/**
 * Represents the React Native View holder for the Nitro "SheetView" HybridView.
 */
@interface HybridSheetViewComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridSheetViewComponent {
  std::shared_ptr<HybridSheetViewSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridSheetViewComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridSheetViewComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridSheetViewSpec> hybridView = RNSwiftUI::RNSwiftUIAutolinking::createSheetView();
    _hybridView = std::dynamic_pointer_cast<HybridSheetViewSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  RNSwiftUI::HybridSheetViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridSheetViewProps const>(props);
  auto& newViewProps = const_cast<HybridSheetViewProps&>(newViewPropsConst);
  RNSwiftUI::HybridSheetViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // isPresented: optional
  if (newViewProps.isPresented.isDirty) {
    swiftPart.setIsPresented(newViewProps.isPresented.value);
    newViewProps.isPresented.isDirty = false;
  }
  // detents: optional
  if (newViewProps.detents.isDirty) {
    swiftPart.setDetents(newViewProps.detents.value);
    newViewProps.detents.isDirty = false;
  }
  // title: optional
  if (newViewProps.title.isDirty) {
    swiftPart.setTitle(newViewProps.title.value);
    newViewProps.title.isDirty = false;
  }
  // message: optional
  if (newViewProps.message.isDirty) {
    swiftPart.setMessage(newViewProps.message.value);
    newViewProps.message.isDirty = false;
  }
  // primaryButtonTitle: optional
  if (newViewProps.primaryButtonTitle.isDirty) {
    swiftPart.setPrimaryButtonTitle(newViewProps.primaryButtonTitle.value);
    newViewProps.primaryButtonTitle.isDirty = false;
  }
  // secondaryButtonTitle: optional
  if (newViewProps.secondaryButtonTitle.isDirty) {
    swiftPart.setSecondaryButtonTitle(newViewProps.secondaryButtonTitle.value);
    newViewProps.secondaryButtonTitle.isDirty = false;
  }
  // onNativeDismiss: optional
  if (newViewProps.onNativeDismiss.isDirty) {
    swiftPart.setOnNativeDismiss(newViewProps.onNativeDismiss.value);
    newViewProps.onNativeDismiss.isDirty = false;
  }
  // onNativePrimaryAction: optional
  if (newViewProps.onNativePrimaryAction.isDirty) {
    swiftPart.setOnNativePrimaryAction(newViewProps.onNativePrimaryAction.value);
    newViewProps.onNativePrimaryAction.isDirty = false;
  }
  // onNativeSecondaryAction: optional
  if (newViewProps.onNativeSecondaryAction.isDirty) {
    swiftPart.setOnNativeSecondaryAction(newViewProps.onNativeSecondaryAction.value);
    newViewProps.onNativeSecondaryAction.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return RNSwiftUI::RNSwiftUIAutolinking::isSheetViewRecyclable();
}

- (void)prepareForRecycle {
  [super prepareForRecycle];
  RNSwiftUI::HybridSheetViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

@end
