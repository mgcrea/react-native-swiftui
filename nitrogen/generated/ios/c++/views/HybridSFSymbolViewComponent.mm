///
/// HybridSFSymbolViewComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridSFSymbolViewComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridSFSymbolViewSpecSwift.hpp"
#import "RNSwiftUI-Swift-Cxx-Umbrella.hpp"

#if __has_include(<cxxreact/ReactNativeVersion.h>)
#include <cxxreact/ReactNativeVersion.h>
#if REACT_NATIVE_VERSION_MINOR >= 82
#define ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
#endif
#endif

using namespace facebook;
using namespace margelo::nitro::rnswiftui;
using namespace margelo::nitro::rnswiftui::views;

/**
 * Represents the React Native View holder for the Nitro "SFSymbolView" HybridView.
 */
@interface HybridSFSymbolViewComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridSFSymbolViewComponent {
  std::shared_ptr<HybridSFSymbolViewSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridSFSymbolViewComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridSFSymbolViewComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridSFSymbolViewSpec> hybridView = RNSwiftUI::RNSwiftUIAutolinking::createSFSymbolView();
    _hybridView = std::dynamic_pointer_cast<HybridSFSymbolViewSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  RNSwiftUI::HybridSFSymbolViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridSFSymbolViewProps const>(props);
  auto& newViewProps = const_cast<HybridSFSymbolViewProps&>(newViewPropsConst);
  RNSwiftUI::HybridSFSymbolViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // symbolName: string
  if (newViewProps.symbolName.isDirty) {
    swiftPart.setSymbolName(newViewProps.symbolName.value);
    newViewProps.symbolName.isDirty = false;
  }
  // size: optional
  if (newViewProps.size.isDirty) {
    swiftPart.setSize(newViewProps.size.value);
    newViewProps.size.isDirty = false;
  }
  // textStyle: optional
  if (newViewProps.textStyle.isDirty) {
    swiftPart.setTextStyle(newViewProps.textStyle.value);
    newViewProps.textStyle.isDirty = false;
  }
  // weight: optional
  if (newViewProps.weight.isDirty) {
    swiftPart.setWeight(newViewProps.weight.value);
    newViewProps.weight.isDirty = false;
  }
  // scale: optional
  if (newViewProps.scale.isDirty) {
    swiftPart.setScale(newViewProps.scale.value);
    newViewProps.scale.isDirty = false;
  }
  // renderingMode: optional
  if (newViewProps.renderingMode.isDirty) {
    swiftPart.setRenderingMode(newViewProps.renderingMode.value);
    newViewProps.renderingMode.isDirty = false;
  }
  // variableValue: optional
  if (newViewProps.variableValue.isDirty) {
    swiftPart.setVariableValue(newViewProps.variableValue.value);
    newViewProps.variableValue.isDirty = false;
  }
  // colors: optional
  if (newViewProps.colors.isDirty) {
    swiftPart.setColors(newViewProps.colors.value);
    newViewProps.colors.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return RNSwiftUI::RNSwiftUIAutolinking::isSFSymbolViewRecyclable();
}

- (void)prepareForRecycle {
  [super prepareForRecycle];
  RNSwiftUI::HybridSFSymbolViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

#ifdef ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
- (void)invalidate {
  RNSwiftUI::HybridSFSymbolViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.onDropView();
  [super invalidate];
}
#endif

@end
