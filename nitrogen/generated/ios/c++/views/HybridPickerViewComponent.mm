///
/// HybridPickerViewComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridPickerViewComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridPickerViewSpecSwift.hpp"
#import "RNSwiftUI-Swift-Cxx-Umbrella.hpp"

#if __has_include(<cxxreact/ReactNativeVersion.h>)
#include <cxxreact/ReactNativeVersion.h>
#if REACT_NATIVE_VERSION_MINOR >= 82
#define ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
#endif
#endif

using namespace facebook;
using namespace margelo::nitro::rnswiftui;
using namespace margelo::nitro::rnswiftui::views;

/**
 * Represents the React Native View holder for the Nitro "PickerView" HybridView.
 */
@interface HybridPickerViewComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridPickerViewComponent {
  std::shared_ptr<HybridPickerViewSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridPickerViewComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridPickerViewComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridPickerViewSpec> hybridView = RNSwiftUI::RNSwiftUIAutolinking::createPickerView();
    _hybridView = std::dynamic_pointer_cast<HybridPickerViewSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  RNSwiftUI::HybridPickerViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridPickerViewProps const>(props);
  auto& newViewProps = const_cast<HybridPickerViewProps&>(newViewPropsConst);
  RNSwiftUI::HybridPickerViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // value: optional
  if (newViewProps.value.isDirty) {
    swiftPart.setValue(newViewProps.value.value);
    newViewProps.value.isDirty = false;
  }
  // selection: optional
  if (newViewProps.selection.isDirty) {
    swiftPart.setSelection(newViewProps.selection.value);
    newViewProps.selection.isDirty = false;
  }
  // label: optional
  if (newViewProps.label.isDirty) {
    swiftPart.setLabel(newViewProps.label.value);
    newViewProps.label.isDirty = false;
  }
  // labelColor: optional
  if (newViewProps.labelColor.isDirty) {
    swiftPart.setLabelColor(newViewProps.labelColor.value);
    newViewProps.labelColor.isDirty = false;
  }
  // options: optional
  if (newViewProps.options.isDirty) {
    swiftPart.setOptions(newViewProps.options.value);
    newViewProps.options.isDirty = false;
  }
  // pickerStyle: optional
  if (newViewProps.pickerStyle.isDirty) {
    swiftPart.setPickerStyle(newViewProps.pickerStyle.value);
    newViewProps.pickerStyle.isDirty = false;
  }
  // controlSize: optional
  if (newViewProps.controlSize.isDirty) {
    swiftPart.setControlSize(newViewProps.controlSize.value);
    newViewProps.controlSize.isDirty = false;
  }
  // disabled: optional
  if (newViewProps.disabled.isDirty) {
    swiftPart.setDisabled(newViewProps.disabled.value);
    newViewProps.disabled.isDirty = false;
  }
  // onNativeChange: optional
  if (newViewProps.onNativeChange.isDirty) {
    swiftPart.setOnNativeChange(newViewProps.onNativeChange.value);
    newViewProps.onNativeChange.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return RNSwiftUI::RNSwiftUIAutolinking::isPickerViewRecyclable();
}

- (void)prepareForRecycle {
  [super prepareForRecycle];
  RNSwiftUI::HybridPickerViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

#ifdef ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
- (void)invalidate {
  RNSwiftUI::HybridPickerViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.onDropView();
  [super invalidate];
}
#endif

@end
